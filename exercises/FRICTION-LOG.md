# FRICTION-LOG.md

Objections held back so that a choice could play out, per the lessons skill.
Each entry records the choice, the objection, the cost it predicts, and where
to watch for it. Status is `pending`, `landed` or `never landed`. The
`never landed` entries are kept: they are what calibrates future objections.

*(none yet)*
